# @journeyid/prettier-config

## Usage

Install the package:

````bash
```bash
yarn add --dev @journeyid/prettier-config
````

Edit `package.json`:

```jsonc
{
  // ...
  "prettier": "@journeyid/prettier-config"
}
```
